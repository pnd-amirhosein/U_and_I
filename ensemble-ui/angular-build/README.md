# Internal Angular APF build

This folder is build-only source for the precompiled `ensemble-ui/angular` entrypoint. It is not published. Stencil generates `src/generated/*`; `ng-packagr` compiles that source into `dist-angular/`.
